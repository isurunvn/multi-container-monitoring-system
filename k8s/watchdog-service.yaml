apiVersion: v1
kind: Service
metadata:
  name: watchdog-service
  namespace: monitoring
spec:
  type: ClusterIP
  selector:
    app: watchdog
  ports:
    - name: http
      port: 8080
      targetPort: 8080
