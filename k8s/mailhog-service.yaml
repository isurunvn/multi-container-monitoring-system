apiVersion: v1
kind: Service
metadata:
  name: mailhog-service
spec:
  type: NodePort
  selector:
    app: mailhog
  ports:
    - name: smtp
      port: 1025
      targetPort: 1025
      nodePort: 30125
    - name: ui
      port: 8025
      targetPort: 8025
      nodePort: 30825
